# Порядок команд не принциповий

# З якого образа беремо інформацію
FROM openjdk:latest

# Які файли з поточного проекта ми копіюємо в імедж
# Якщо усі файли в корені, то пишемо першу крапку, або назву папки
# Другий параметр - в яку папку в образі ми це поміщаємо, крапка якщо в корінь
COPY . /java

# Вказуємо рабочу діректорію, в данному випадку ту ж саму
WORKDIR /java

# Вказуємо порт, щоб запустити у браузері(в джаві не працює)
EXPOSE 8001

# Команда виконувана при першому запуску контейнера, при створенні контейнера
RUN javac Main.java
# javac - компайлер який запускає файл меін.джава

# Команда "java Main" виконується кожен раз при запуску контейнера:
CMD ["java", "Main"]